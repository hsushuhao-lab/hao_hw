# CK GitHub web assets

This branch is intentionally self-contained with lightweight SVG web assets so the prototype runs directly from static hosting.

The full-resolution concept art (JPG + PNG) and cropped production-reference portraits are included in the downloadable `CK_v0.1.zip` release package produced with this milestone.

When production sprite sheets are approved, replace these SVG fallbacks without changing gameplay IDs.
